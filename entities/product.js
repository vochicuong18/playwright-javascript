export default class Product {
  constructor(name, price, url, category, subCategory) {
    this.name = name;
    this.price = price;
    this.url = url;
    this.category = category;
    this.subCategory = subCategory
  }
}